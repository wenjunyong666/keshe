# 遥控器工程交接整合说明

## 当前基线

本目录以 `D:\keshe\REMOTE_F401_V3.2` 为基准复制得到，再按任务书要求继续开发。

## 为什么不直接覆盖旧工程

`D:\keshe2\REMOTE_F401_V3.2任务1、2` 里已经写过一部分 OLED 页面和锁图标显示，但核心控制逻辑仍大量回退到了旧版遥控器实现，和任务书要求不完全一致。

因此本次整合采用：

- 保留基准工程中已经较完整的任务书功能主逻辑
- 尽量保留同学已经写过的 OLED 展示痕迹和函数名
- 不强行把明显不完整或会破坏现有功能的代码整体搬过来

## 已保留的同学代码入口

已在 `Core/Src/main.c` 中保留以下兼容函数名，便于后续继续接手：

- `OLED_DrawIcon()`
- `DisplayLocked()`
- `DisplayUnlocked()`
- `Show_Main_Menu()`
- `Show_Sleep_Set_Menu()`
- `Show_Weitiao_Set_Menu()`

同时保留了一份锁图标字模数据，作为交接痕迹。

## 后续建议

后续继续开发时，建议优先在当前工程上改：

- `Sources/remote.c`
- `Sources/remote.h`
- `Core/Src/main.c`

不要再把 `REMOTE_F401_V3.2任务1、2` 整体覆盖回来，否则容易把已经适配好的功能重新冲掉。
